---
title: Title From Frontmatter
author: Sahu, S
---

# Heading

Rendered with `--title` so the frontmatter title and author reach the PDF.
Metadata keys must be lowercase — pandoc's `title`/`author` variables are
case-sensitive, so `Title:` silently renders nothing.
